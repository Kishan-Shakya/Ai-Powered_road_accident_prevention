services:
  - name: ai-road-accident-dashboard
    type: web
    runtime: node
    plan: free
    rootDir: Dashboard

    buildCommand: npm install && npm run build
    startCommand: npm start

    envVars:
      - key: NODE_ENV
        value: production
